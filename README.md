# mneme
mneme — Persistent memory system for AI coding agents. Single binary, zero dependencies. Hybrid retrieval (BM25 + vector + graph), automatic consolidation, multi-scope memory, and agent-agnostic via MCP.
