---
id: 019f6be5-3a70-74a1-b520-ca956dba8336
type: discovery
scope: project
title: "SPEC-090 QA verdict: APPROVED — cero observaciones, gate ejecutado independientemente"
topic_key: spec/SPEC-090-qa-verdict
project: wirvii/mneme
importance: 0.60
confidence: 0.80
decay_rate: 0.02
created_at: 2026-07-16T17:06:52.656501Z
updated_at: 2026-07-16T17:06:52.656501Z
revision_count: 0
files:
  - internal/subagents/layer23.go
  - internal/subagents/layer23_test.go
  - internal/subagents/foreign.go
  - internal/subagents/foreign_test.go
  - internal/mcp/handlers_subagents.go
  - internal/mcp/handlers_subagents_test.go
  - internal/cli/subagents_doctor.go
  - internal/install/assets/skills/mneme-init/SKILL.md
  - internal/install/assets/skills/mneme-init/validation/run.sh
  - internal/install/skills_layer23_test.go
shared: 1
author: Juan Fernando Tamayo Puertas <jftamayop@gmail.com>
---

QA independiente (qa-tester) sobre `feat/spec-090-layer23-boundary` (7 commits, 3556eb9..25dc72b), base 0a5b56d (v1.27.2/main). VEREDICTO: APROBADO, cero observaciones.

## G2/AC4 (el punto crítico) — verificado con mutación real, no solo lectura
Leí `handleSubagentWrite`: usa `subagents.ExtractGrillRegion(req.ComposedMD)` y escanea SOLO la región extraída, nunca `req.ComposedMD` completo. `TestSubagentWrite_AcceptsLayer1ProhibitionOutsideRegion` compone un perfil REAL vía `subagent_compose` (usa el asset real `internal/subagents/assets/agent-fixed.md`, que en su línea 109 contiene literalmente "NUNCA llames `spec_advance`"), confirma por precondición que el texto está en `composed.ComposedMD` pero NO en la región extraída, y afirma aceptación. Hice la mutación yo mismo (worktree del repo principal, restaurado byte-idéntico después): cambié el guard para escanear `req.ComposedMD` en vez de `region` → el test se puso ROJO reproduciendo exactamente el "guardián ciego opuesto" que G2 existe a prevenir (error: contiene "spec_advance" línea 57, "tools:"/"permissionMode:" del frontmatter). Confirmado: el diseño es correcto Y el test discrimina de verdad.

## Resto de guardianes (G1, G3-G7) — verificados por lectura de código y tests
- G1 compose/write: tokens literales reales ("spec_advance" en `areas_layer3_md`/región inyectada), no constantes comparadas consigo mismas. AC2/AC3: `os.Stat` confirma que nada se escribe a disco en rechazo.
- G3 doctor: `diagnoseManifestEntry` (CLI) y `diagnoseManifestEntryMCP` (MCP) llaman AMBOS `subagents.DetectLayer23Leaks(e.Path, readContent)` — un solo sitio leaf, sin duplicación de 30 líneas. Report-only confirmado (solo agrega finding, nunca modifica/elimina).
- G4: `ExtractGrillRegion(wrapUntrustedAreasContent(x)) == x` confirmado por `TestExtractGrillRegion_RoundTrip` + variante embebida en perfil completo.
- G5: hice la mutación yo mismo — borré las menciones de spec_advance/spec_quick/spec_reject de SKILL.md → tanto `validation/run.sh` (exit 1) COMO `TestMnemeInitSkill_DocumentsEveryLayer23ForbiddenToken` (FAIL, 3 aserciones) se pusieron rojos simultáneamente. Restaurado byte-idéntico (`git diff --stat` vacío después). El test Go lee `subagents.Layer23ForbiddenLifecycleTokens` directamente (el símbolo real), no una lista duplicada.
- G6: tabla de 4 casos en `TestIsForeignAgentFile_G6` discrimina ambos sentidos (con/sin bloque agent-fixed, vacío, mención en prosa sin marcador real).
- G7: `TestSubagentWrite_BacksUpDevOwnedFile` confirma backup byte-idéntico (`string(backedUp) != string(original)`) solo cuando `existed && subagents.IsForeignAgentFile(...)`; `TestSubagentWrite_NoBackupForOwnFile` confirma ausencia de backup en regeneración propia.

## AC7/AC9 confirmados
- AC7: `detectForeignAgents` excluye agentes de mneme (bloque agent-fixed presente Y rol conocido en manifest) — probado con fixture mixta real (uno propio vía compose+write, uno foráneo hand-authored) en el MISMO test.
- AC9: `internal/enforcement` diff vacío (`git diff 0a5b56d..HEAD -- internal/enforcement` sin salida); `PermissionTable`/`composer.go` sin tocar; `TestSubagentWrite_RejectsPermissionEscalationInComposedMD` sigue verde sin modificación.

## Gate ejecutado independientemente
- `go clean -testcache && go test ./...`: 34 paquetes verdes (fresh, no cache).
- `go test -race ./...`: verde completo (`internal/service` 249s, sin flake esta corrida).
- `~/go/bin/golangci-lint run ./...`: 0 issues.
- `go vet ./...`: limpio.
- 0 firmas de Claude/Anthropic en los 7 commits (grep en mensajes de commit).
- Cobertura: `internal/model` 100%, `internal/scoring` 97.3%, `internal/subagents` (paquete nuevo, leaf) 94.7%, `internal/service` 71.0%/`internal/store` 70.3% (idéntico a niveles pre-existentes de main, ningún código nuevo en esos paquetes en esta rama — diff --stat confirma cero archivos tocados en store/service).
- `gofmt -l` marca `internal/mcp/handlers_subagents.go` — confirmado BYTE-IDÉNTICO al drift ya presente en main (diff entre `gofmt -d` de main vs rama: idéntico, mismo problema de alineación de struct tags en 2 structs) — pre-existente, no introducido, golangci-lint no lo señala.
- MCP se mantiene en 65 tools (`case "..."` count en handlers.go), 6 `subagent_*` sin cambios de superficie — solo `foreign_agents` como campo nuevo en la respuesta de `subagent_fingerprint`.
- BL-102 (refined) y BL-108 (raw) confirmados NO promovidos — no se colaron en el scope de SPEC-090. BL-110 permanece "refined" (pendiente de cierre por el orquestador tras done).
- `mneme skills lint mneme-init` → passed=true. `validation/run.sh` ejecutado directo → exit 0.

## R5 (honestidad del límite)
Confirmado en el código: `ScanLayer23Leaks`'s godoc declara explícitamente "deliberately NOT exhaustive: it never catches paraphrased doctrine... see SPEC-090 D4's documented honesty about that semantic limit." No lo reporto como defecto — es diseño aceptado por el owner, y el límite está documentado en el propio símbolo de producción, no escondido.

No se llamó spec_advance ni spec_reject — reporto y paro, según instrucción.
