---
id: 019f6b2f-0b78-7496-b3fa-5488ac14ef89
type: discovery
scope: project
title: "SPEC-089 QA verdict: APPROVED — 3 commits, cero observaciones"
project: wirvii/mneme
importance: 0.60
confidence: 0.80
decay_rate: 0.02
created_at: 2026-07-16T13:47:53.080118Z
updated_at: 2026-07-16T13:47:53.080118Z
revision_count: 0
files:
  - internal/subagents/manifest_path.go
  - internal/service/teammemory_import.go
  - internal/service/vault_import.go
  - internal/service/teammemory.go
  - internal/cli/subagents.go
  - internal/cli/subagents_doctor.go
  - internal/mcp/handlers_subagents.go
  - internal/cli/hook_pathowned_test.go
shared: 1
author: Juan Fernando Tamayo Puertas <jftamayop@gmail.com>
---

QA independiente (qa-tester) sobre `fix/spec-089-manifest-paths` (db5b371, 6248c7f, d3654d3), base e8f04f6 (v1.27.1/main). VEREDICTO: APROBADO, cero observaciones.

## El hallazgo del backend — verificado con lupa, CONFIRMADO real y crítico
- `codegraph_callers(ImportFromShared)` + lectura directa de `internal/cli/team_memory_hooks.go:235` confirman: `mneme team-memory hooks run-import` (instalado como git hook `post-merge`/`post-checkout`, ver `teamMemoryHooksManagedBlock`) llama `ImportFromShared` → `importSharedNote` (teammemory_import.go). Esta es la ruta REAL del incidente (`git pull` automático).
- `importNote`/`VaultImport` (vault_import.go) es una función DISTINTA, sin código compartido, solo alcanzable vía el comando manual `mneme vault import`.
- Confirmado con mutación real (worktree aislado, no toqué el repo principal): removí el guard `if fm.TopicKey == SubagentManifestTopicKey` de `importSharedNote` — el test `TestImportFromShared_SkipsSubagentManifestNote_LocalManifestSurvives` se puso ROJO reproduciendo el clobber exacto (Updated=1, contenido foráneo "bug-hunter"/"chateaprov3" visible en el manifest local). Hice lo mismo independientemente en `importNote` (vault_import.go) — también rojo. Cada guard se verifica de forma independiente: quitar uno no afecta al otro.
- Ambos guards están ANTES del branch `existing != nil` (el que hace `svc.Update` y clobbea) — cubren la sobrescritura, no solo la creación. El fixture de `importSharedNote` reutiliza intencionalmente el MISMO id del manifest local con timestamp más nuevo, exactamente el escenario real.
- Respuesta a "¿si solo hubiera parcheado importNote, seguiría vivo el bug real?": SÍ — confirmado, son funciones completamente separadas.

## Guardián 4 (fixture heterogénea, D4/AC8) — verificado
`TestSave_TeamMemoryActive_SubagentManifestNeverMaterializes`: manifest (Shared=0, no materializa, os.Stat real) + ProjectProfile control (Shared=1, sí materializa) en el MISMO test. Mutación real: quité `if topicKey == SubagentManifestTopicKey { return 0 }` de `bakeSharedDefault` → test rojo (manifest Shared=1, materializado). Discrimina de verdad — no es el antipatrón de fixture homogénea de SPEC-085 (mem 019f686b).
Exclusión confirmada por topic_key, no por tipo: `bakeSharedDefault` chequea `topicKey == SubagentManifestTopicKey` ANTES de `autoSharedType(t)` — otros type=config siguen compartiendo.

## Parte 1 (AC1/AC2) — evidencia real, no mocks
`TestRegenerateManifestEntries_NovoFixture_RejectsForeignChateaprov3Path`: tempdir hermano REAL (`chateaprov3Root := t.TempDir()`), assertions con `os.Stat`/`os.ReadFile` sobre el path real — nunca tocado. `TestRegenerateManifestEntries_VentasWpDropiFixture_RejectsAllWindowsPaths`: 4 rutas `c:\Users\...`, `os.ReadDir(root)` confirma CERO ficheros escritos. `isForeignManifestPath` detecta backslash/UNC/drive-letter explícitamente (no confía en `filepath.IsAbs`, que da `false` para paths Windows en darwin) — hay un test dedicado (`TestResolveManifestPath_MutationGuard_ForeignDetection`) que prueba justo esa trampa.

## Parte 2 (AC6/AC7) — verificado
AC6: fichero on-disk byte-idéntico, solo `Path` persistido migra a relativo; `changedAny` se activa también solo por `pathMigrated` (si no, un legacy absoluto-in-repo con contenido ya vigente NUNCA migraría). AC7: diff vacío en `internal/cli/hook.go`+`internal/enforcement` en las 3 commits (confirmado con `git diff e8f04f6..d3654d3`), `hookManifestEntry` no declara campo `Path` — `TestResolvePathOwnership_IgnoresPathRepresentation` prueba que dos manifests idénticos salvo el shape de Path (absoluto/relativo) dan la MISMA decisión.

## Paridad CLI/MCP
`internal/cli/subagents_doctor.go` y `internal/mcp/handlers_subagents.go` mantienen diagnoseManifestEntry/diagnoseManifestEntryMCP como espejos exactos (foreign_path checked antes de orphan_path/drift, mutuamente exclusivos en ambos). subagent_write (MCP) y subagents write (CLI) persisten Path relativo vía `manifestRelativePath` idéntica en ambos.

## Gate ejecutado independientemente
- `go build ./...`: OK.
- `go test -tags fts5 ./...`: 34 paquetes OK (repetí el run completo).
- `golangci-lint run ./...`: 0 issues.
- `go vet ./...`: limpio.
- `go test -race ./internal/subagents/... ./internal/cli/... ./internal/mcp/...`: limpio.
- `go test -race ./internal/service/...`: limpio, 244.965s (coincide con lo reportado por backend).
- `gofmt -l` en ficheros tocados: solo `internal/mcp/handlers_subagents.go` con drift — confirmado BYTE-IDÉNTICO al drift ya presente en `main` (diff vacío entre `gofmt -d` de ambas versiones) — pre-existente, no introducido, no bloqueante (golangci-lint no lo marca).
- Cobertura de paquete: `internal/service` 71.0% y `internal/store` 70.3% en la rama — IDÉNTICO a main (e8f04f6: 70.9%/70.3%) — no es regresión de este PR, es condición pre-existente de paquetes grandes. `internal/subagents` (el paquete nuevo, leaf) 94.2%.
- `internal/subagents/manifest_path.go`: leaf real, solo importa `path/filepath`, `runtime`, `strings` (stdlib).
- 0 firmas de Claude/Anthropic en los 3 commits (grep preciso, sin falsos positivos).
- go.mod/go.sum sin cambios. Sin migraciones. Sin cmd/ tocado.
- BL-102/104/108/110/112: los 5 siguen `refined`/`raw`, ninguno `promoted` — no se colaron en el scope de SPEC-089.

## Único hallazgo no-bloqueante (ya documentado, no de este PR)
gofmt drift en handlers_subagents.go es pre-existente en main, verificado byte-a-byte idéntico al de la rama — no bloqueante, no de este trabajo.

No se llamó spec_advance ni spec_reject — reporto y paro, según instrucción.
