---
id: 019f6bd9-219d-749f-8057-4f5985d3fa76
type: decision
scope: project
title: "SPEC-090 done: la frontera de la capa 2/3 (BL-110 cerrado, BL-112 core)"
topic_key: spec/SPEC-090-done
project: wirvii/mneme
importance: 0.85
confidence: 0.80
decay_rate: 0.005
created_at: 2026-07-16T16:53:39.869631Z
updated_at: 2026-07-16T16:53:39.869631Z
revision_count: 0
files:
  - internal/subagents/layer23.go
  - internal/subagents/foreign.go
  - internal/mcp/handlers_subagents.go
  - internal/cli/subagents_doctor.go
  - internal/install/assets/skills/mneme-init/SKILL.md
  - internal/install/skills_layer23_test.go
shared: 1
author: Juan Fernando Tamayo Puertas <jftamayop@gmail.com>
---

SPEC-090 implementado en rama `feat/spec-090-layer23-boundary` (7 commits, 3556eb9..25dc72b), sobre v1.27.2. NO merge/push/tag — pendiente del owner. NO se llamó `spec_advance`.

## El invariante
Capa 1 = quién ES el agente y qué PUEDE hacer (Go-authored). Capa 2/3 (región de grill entre `GrillContentWrapStart/End`) = SOLO conocimiento de proyecto — nunca lifecycle (`spec_advance`/`spec_quick`/`spec_reject`), nunca capacidades (`tools:`/`permissionMode:`), nunca doctrina.

## Parte A (commits 1-4, cierra BL-110)
1. `internal/subagents/layer23.go`: `ScanLayer23Leaks`, `Layer23ForbiddenLifecycleTokens` (exportado), `GrillContentWrapStart/End` movidos desde `internal/mcp/handlers_subagents.go` (única fuente), `ExtractGrillRegion`.
2. Guard REJECT en `handleSubagentCompose` (escanea `req.AreasLayer3MD` raw) y `handleSubagentWrite` (extrae la región de `req.ComposedMD` con `ExtractGrillRegion`, escanea SOLO eso — G2, la propiedad crítica).
3. `DetectLayer23Leaks(path, readContent)` helper compartido; finding `doctorKindLifecycleInLayer23`/`mcpDoctorKindLifecycleInLayer23` (report-only) en ambas copias del doctor, `readContent` inyectado en paralelo a `actualChecksum`.
4. SKILL.md 1.3.0→1.4.0: Critical Rule 11 + fila Automated Checks + `validation/run.sh` afirma cada token. Acople no-ciego reforzado con test Go a nivel de repo (`internal/install/skills_layer23_test.go`, lee `Layer23ForbiddenLifecycleTokens` directamente — más robusto que el run.sh estático).

## Parte B (commits 5-7, BL-112 core)
5. `subagents.IsForeignAgentFile(content)` (leaf, usa `managedblock.ReadText`) + campo `foreign_agents []string` en `subagentFingerprintResponse` (sin tool nuevo) — foreign si falta el bloque agent-fixed O si el rol no está en el manifest.
6. Backup `.bak-<UTC-timestamp>` en `handleSubagentWrite` ANTES de sobrescribir un fichero del dev (con marcador ausente); `BackupPath` en `subagentWriteResponse`; sin backup al regenerar un fichero nuestro.
7. SKILL.md 1.4.0→1.5.0: prosa de fusión (Fase 0.5) — detección vía `foreign_agents`, solapamiento por juicio LLM (sin heurística de nombres), síntesis por EXTRACCIÓN nunca concatenación (el guard de la Parte A es la red), adopción como rol custom.

## Las 7 mutaciones verificadas (romper→rojo→restaurar byte-idéntico)
- G1 compose: detector→nil rojo en `TestSubagentCompose_RejectsLayer23Leak`.
- G1 write: detector→nil rojo en `TestSubagentWrite_RejectsLayer23LeakInRegion`.
- G2 (crítica): escanear el `composed_md` completo en vez de solo la región extraída pone `TestSubagentWrite_AcceptsLayer1ProhibitionOutsideRegion` en rojo — confirma que sin `ExtractGrillRegion` la prohibición legítima de capa 1 se rechazaría para siempre.
- G3: detector→nil rojo en ambas copias del doctor simultáneamente.
- G4: desincronizar la constante que `ExtractGrillRegion` busca de la que el wrap usa rompe `TestExtractGrillRegion_RoundTrip`.
- G5: dos mutaciones — (A) 4º token sin documentar rompe el test Go de acople; (B) borrar la Critical Rule 11 + fila de Automated Checks rompe tanto `validation/run.sh` COMO el test Go simultáneamente.
- G6: invertir `!present`→`present` en `IsForeignAgentFile` rompe los 4 casos de la tabla (ambos sentidos de la clasificación).
- G7: quitar la condición `&& subagents.IsForeignAgentFile(...)` produce un backup espurio al regenerar un fichero propio, detectado por `TestSubagentWrite_NoBackupForOwnFile`.

## AC4/AC9 confirmados explícitamente
- AC4 (G2): compose/write con la prohibición SOLO en capa 1 y región limpia → aceptado. `TestSubagentWrite_AcceptsLayer1ProhibitionOutsideRegion` lo prueba directamente.
- AC9: `PermissionTable` inalterado (no tocado en ningún commit); `TestSubagentWrite_RejectsPermissionEscalationInComposedMD` (regresión SPEC-057) sigue en verde sin modificación.

## Verificación
`make test`/`go test ./...` verde (35 paquetes). `go test -race ./...` verde en 2 corridas completas (la primera tuvo un FAIL transitorio en `internal/service` bajo contención de recursos con 35 paquetes corriendo `-race` en paralelo — `internal/service` NO fue tocado por esta rama, y tanto la corrida aislada como una segunda corrida completa pasaron limpio, confirmando que fue un flake ambiental, no una regresión). `~/go/bin/golangci-lint run ./...` 0 issues. MCP se mantiene en 65 tools (el brief original decía "64" pero ese ya era el conteo pre-SPEC-087; no se registró ningún tool nuevo — `subagent_fingerprint` solo ganó un campo de respuesta). `gofmt -l` marca varios ficheros incl. `handlers_subagents.go` por una alineación de struct tags que YA existía en `main` antes de esta spec (confirmado con `git show main:... | gofmt -l`) — no introducido por SPEC-090, y `golangci-lint` (el gate autoritativo del proyecto) no lo señala.

Ver también [[testing/antipatron-guardian-que-no-detecta-su-eliminacion]] — las 7 mutaciones de esta spec siguen exactamente esa disciplina.
