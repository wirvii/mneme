// Package main_test provides a build-time smoke test for the mneme binary.
package main

import "testing"

// TestBuild is a placeholder that verifies the mneme binary compiles correctly.
// The real build verification happens via "go build -tags fts5 ./cmd/mneme".
func TestBuild(t *testing.T) {
	// If this file compiles, the binary compiles. No runtime assertions needed.
}
